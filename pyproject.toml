[project]
name = "suzuke"
version = "0.1.0"
description = "Beautiful AI assistant in the terminal"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "g4f>=7.1.1",
    "platformdirs>=4.5.1",
    "textual>=7.3.0",
]
